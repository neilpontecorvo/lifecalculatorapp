# Agent Instructions
Inspect Git status and project structure before editing. Preserve the Phase 1 wheel behavior and use `CamelotCatalog` as the single canonical key source; never duplicate key mappings. Run `./gradlew testDebugUnitTest`, `./gradlew lintDebug`, and `./gradlew assembleDebug` before delivery when Android dependencies are reachable. Never claim device installation, connected tests, screenshots, or launch verification unless the command actually succeeded. Do not commit secrets, generated binaries, signing keys, local.properties, Gradle caches, or build directories. Maintain extension points for future audio analysis, library, and route-planning phases without implementing them in Phase 1. Document changed files and unresolved environment issues.
