# ADR-001: Compose Canvas Wheel
Use a programmatic Jetpack Compose Canvas wheel rather than a static image. This keeps rendering, labels, touch hit testing, responsiveness, and accessibility semantics deterministic and maintainable.
